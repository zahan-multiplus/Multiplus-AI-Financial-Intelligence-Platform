# Documentation viewer (moved to repo root)

The documentation viewer is now the **site root**. Open the project root in a browser or run:

```bash
python -m http.server 8000
```

Then open **http://localhost:8000/** (not /viewer/).

See the main [README.md](../README.md) in the project root for the current structure.
